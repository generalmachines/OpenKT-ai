# System architecture

![OpenKT system architecture](system.svg)

- `system.excalidraw` — open it at [excalidraw.com](https://excalidraw.com) (File → Open) to edit.
- `system.svg` — the image the READMEs show.
- `build.py` — both files come from one scene description: `python3 docs/architecture/build.py docs/architecture`.

Dashed boxes are designed but not built yet. See [docs/architecture.md](../architecture.md) for the full design and `docs/specs/` for the decided details.
