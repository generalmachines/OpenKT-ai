{
  "name": "@openkt/recall",
  "version": "0.1.0",
  "description": "Pure ranking functions for OpenKT recall (Spec 01 section 4): weights, fusion, abstain, diversity, budget, plus the rerank HTTP client.",
  "license": "Apache-2.0",
  "repository": { "type": "git", "url": "https://github.com/masti-ai/openkt-next.git", "directory": "packages/recall" },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }, "./package.json": "./package.json" },
  "files": ["dist", "README.md"],
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": { "@types/node": "^22.10.0", "typescript": "^5.7.0", "vitest": "^3.0.0" }
}
