{
  "name": "@openkt/pipeline",
  "version": "0.1.0",
  "description": "Pure decision functions for the OpenKT write path (Spec 02): chunking, duplicate rules, tag normalising, routing guards, section validation, confidence. No database, no network, no clock.",
  "license": "Apache-2.0",
  "repository": { "type": "git", "url": "https://github.com/masti-ai/openkt-next.git", "directory": "packages/pipeline" },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }, "./package.json": "./package.json" },
  "files": ["dist", "README.md"],
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": { "@types/node": "^22.10.0", "typescript": "^5.7.0", "vitest": "^3.0.0" }
}
