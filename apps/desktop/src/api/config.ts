/** The one place the default server lives. The owner's test server (Tailscale). */
export const DEFAULT_SERVER_URL = 'http://pratham-gpu.taile8dc37.ts.net:3300';

/** Personal access tokens issued by `POST /v1/me/tokens` start with this. */
export const TOKEN_PREFIX = 'okt_pat_';
